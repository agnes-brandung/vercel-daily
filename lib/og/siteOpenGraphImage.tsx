/** Matches Facebook / Open Graph recommended aspect ratio (used from `generateMetadata` / Metadata API). */
export const ogImageSize = { width: 1200, height: 630 };

export const ogFallbackArticleImageSrc = '/vercel-logo.svg';
